{{#each type}}
   {{#each this.BITMAP_FIELDS}}
#define EMBER_AF_{{strong ../label}}_{{strong label}} ({{mask}})
   {{/each}}
{{/each}}

/** @} END Enums */
/** @} END addtogroup */
#endif // SILABS_EMBER_AF_ENUMS